{
  "name": "finance-dashboard-monorepo",
  "version": "1.0.0",
  "description": "Root folder for Finance Dashboard",
  "scripts": {
    "start": "npm run start --prefix backend & npm run dev --prefix client",
    "backend": "npm run dev --prefix backend",
    "client": "npm run dev --prefix client",
    "install-all": "npm install --prefix backend && npm install --prefix client"
  }
}
